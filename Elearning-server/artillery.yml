config:
  target: "http://localhost:5000"
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - name: "Search courses in database"
    flow:
      - get:
          url: "/api/courses/all"
